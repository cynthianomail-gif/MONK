# Codex 生圖 handoff — 香火投擲（賽錢箱）小遊戲美術（共 2 張＋1 張可選）

日期：2026-07-08。香火投擲（OfferingToss）＝飛鏢式力度計小遊戲：夜色神社前，看準力度與
風向把香油錢拋進賽錢箱，正中投入口加倍。目前**整個場景都是程式色塊**（深灰底＋褐色矩形箱
＋金色圓片），本單生正式圖。銅錢**不用生**——沿用化緣關既有的 `beggar/item_coin.png`。

## 落點（新資料夾，交回後由 Claude 加載入掛點接線）

`MONK/assets/2d/minigames/offering/`
- `bg_offering_night.png` — 背景：夜色神社前庭
- `offering_box.png` — 賽錢箱（獨立去背 sprite）
- `gauge_frame.png` — 力度計木框（**可選**，沒空可跳過，程式框仍可用）

檔名照上表，不能變。

## 輸出格式

- `bg_offering_night.png`：**1920×1080 滿版，不透明**。
- `offering_box.png`：**透明底 PNG（去背）**，橫式構圖建議 1024×640，箱體置中佔畫面 80%。
- `gauge_frame.png`（可選）：透明底直式窄條，建議 128×768（遊戲內縮放到 36×300 使用，
  框內留空給程式填充的金色力度條）。

## 構圖鐵則（照 2026-07-03 Codex 自己定的規矩）

**背景只當靜態環境——賽錢箱絕對不要畫進背景**（它要跟程式判定窗對位，必須是獨立 sprite；
之前遊藝場全景圖烤道具造成重疊問題，同一個雷不踩第二次）。背景構圖配合遊戲版面：
- 視角＝參拜者第一人稱，面向本殿方向的夜間前庭；投擲軌跡走畫面中央垂直帶
- **畫面中央 y≈280–560 的水平帶保持乾淨**（賽錢箱 sprite 疊這裡，別放搶戲的細節）
- **右側 x≈1440–1620 直帶避免亮色雜物**（力度計在這）；**左上角**留給 HUD 金字
- 兩側可放：鳥居柱／石燈籠（暖光）／紙垂注連繩／遠景本殿剪影／夜空
- 整體偏暗（現行程式底色是近黑藍 #1A1A21 的調性），暖燈籠光點綴——金色銅錢、金色
  力度條、判定彈字要壓得住底

`offering_box.png` 視角＝**正面微俯**（玩家由下往上拋，略看到箱頂）：木箱體＋箱頂
**直櫺投入口**（柵條間隙，銅錢從縫落入——這是判定目標，柵縫要一眼可辨）＋紅漆邊緣，
可帶「賽銭」墨字牌。輪廓粗、剪影清楚。

## 風格

與遊戲既有水墨線一致：**大神 okami 水墨/半寫實厚塗、game-ready**。
參考錨點：
- 神社街小遊戲同系底圖＝`MONK/assets/2d/minigames/shrine_games/batting_bg_shrine_v2.png`
  與 `bowling_lane_shrine_v2.png`（水墨老街木造調——本關就開在同一條神社街）
- 水墨夜景氛圍＝`MONK/assets/2d/backgrounds/bg_battle_wanhua.png`
- 銅錢長相（要與之相容）＝`MONK/assets/2d/minigames/beggar/item_coin.png`（亮金方孔錢）

## 要生什麼

| 檔名 | 說明 |
|---|---|
| `bg_offering_night.png` | 夜色神社前庭：石板參道向上、兩側石燈籠暖光、鳥居/注連繩入畫、遠景本殿剪影、墨色夜空；中央帶與右側直帶乾淨（見構圖鐵則） |
| `offering_box.png` | 木造賽錢箱正面微俯：深木箱體、箱頂直櫺投入口（柵縫清楚）、紅漆緣、「賽銭」字牌；粗墨線 |
| `gauge_frame.png`（可選） | 直式窄長木牌框，頂部可掛小鈴或紙垂裝飾，框內鏤空 |

## 交回後我（Claude）會做

1. 去背/構圖 QC → 入資料夾 → `OfferingToss.gd` `_build_scene()` 加 `ResourceLoader.exists`
   載入掛點（缺檔仍走程式幾何佔位，零風險）；銅錢節點換 `beggar/item_coin.png` 貼圖。
2. 用網格疊圖法校位：箱體 sprite 對齊判定常數（投入口中心＝(960,380)、半寬 110px）——
   判定窗是常數不是節點位置，只動視覺層不動判定。
3. `--headless --import` → 跑 `TestMinigames` 回歸 → 截圖自檢（銅錢/力度條在新底上的辨識度、
   賽錢箱柵縫與判定窗的視覺對位）。
