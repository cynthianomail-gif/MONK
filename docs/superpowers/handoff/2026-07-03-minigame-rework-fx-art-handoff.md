# Codex 生圖 handoff — 小遊戲玩法重做補件（籌碼＋FX，共 5 張）

日期：2026-07-03
給：Codex（道具/特效美術生成）
落點：`MONK/assets/art_direction/new_ink_shrine_style/minigames/parlor/`（全部放 parlor，FX 雖然保齡球也會用，但統一放這裡好管理）

## 背景：為什麼要這批

三個小遊戲正在做玩法重做（輪盤改「滑鼠點押注格下注、籌碼淡入」、飛鏢改「人中之龍式滑鼠瞄準+力度條」、保齡球改「Wii 式選位置+直球/曲球」）。程式已經全部用「圖不存在就程式畫佔位」的方式寫好，**這批圖交回後不用改程式、檔名對了直接生效**。

## 風格

沿用既有 parlor 紅金黑賭窟調（參考同資料夾的 `roulette_table_top_v2.png`、`blackjack_table_top_v2.png` 的鎏金質感）。FX 類是發光特效，風格上「金色暖光＋水墨韻」即可，不用拘泥寫實。

## 輸出格式

- 道具類：透明底 RGBA PNG，1254×1254，主體置中，比照既有 `*_game_ready.png` 慣例（附 `*_source_chromakey.png` 綠幕源檔）。
- **FX 類（特別注意）**：會用 additive（加法混合）疊在畫面上，所以請畫成**「黑底或透明底上的亮色光效」**——暗部會自動消失、亮部會發光。不要畫在白底或灰底上。

---

## 要生什麼（5 張）

### 籌碼（輪盤下注用，最優先）
| 檔名 | 說明 |
|---|---|
| `chip_100_game_ready.png` | **單枚籌碼正俯視**（正圓、置中），紅底金邊、中央「100」字樣或金色紋樣。玩家點押注格時這枚會淡入疊在格子上，同格可疊多枚（程式會微錯位堆疊），所以邊緣輪廓要乾淨。 |
| `chip_500_game_ready.png` | 同上但金底黑邊、「500」面額。備用面額，第一版玩法先用 100，之後加注額切換直接用。 |

### FX 特效（黑底/透明底亮色光效，additive 疊加用）
| 檔名 | 說明 |
|---|---|
| `fx_impact_burst_game_ready.png` | **通用命中爆點**：放射狀金白色光爆（星芒/墨點飛濺混合），正方形置中。用途＝飛鏢釘中靶面的瞬間、保齡球撞瓶的瞬間，程式會縮放+快速淡出。 |
| `fx_gold_sparkle_game_ready.png` | **贏錢金光**：金色粒子星光群（散開的閃粉/小星芒），偏慶祝感。用途＝輪盤/21點贏錢結算、飛鏢紅心、保齡球全倒時疊在結算文字附近。 |
| `fx_speed_trail_game_ready.png` | **速度尾焰**：一條縱向的暖白色流光（上窄下寬或漸淡），用途＝保齡球出球後跟在球後面拉速度感（輪盤已有 `roulette_ball_trail_fx` 但那條太小，保齡球要大條的）。 |

---

## 交回後我（Claude）會做

程式已寫好 fallback：籌碼目前用程式畫的圓片、FX 目前用程式色塊/粒子頂著。圖放進資料夾（檔名照上表）→ 我跑一次 import → 直接生效，零改碼。

### 附：這批不用生的
- 力度條/瞄準圈/押注格 hover 亮框/確認按鈕＝全程式畫，不用圖。
- 保齡球道/球/瓶、飛鏢靶/鏢、輪盤桌/輪盤/球＝上一批已交，沿用。
